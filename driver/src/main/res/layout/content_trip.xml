<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="io.hypertrack.driver.TripActivity"
    tools:showIn="@layout/activity_trip">

    <Button
        android:id="@+id/start_trip_btn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/start_trip"
        android:onClick="tripStart"/>
    
    <Button
        android:id="@+id/complete_task_btn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/mark_as_complete"
        android:layout_below="@+id/start_trip_btn"
        android:onClick="completeTask"/>

    <Button
        android:id="@+id/end_trip_btn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/end_trip"
        android:onClick="tripEnd"
        android:layout_below="@+id/complete_task_btn" />

</RelativeLayout>
